from distutils.core import setup
import py2exe
import tkinter, urllib.request, os, pathlib, tempfile, subprocess, webbrowser, time, shutil
from PIL import Image, ImageTk
setup(windows=['WSCAppStoreV1.1.py'], py_modules=[])
